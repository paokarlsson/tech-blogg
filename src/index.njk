---
layout: base.njk
title: Hem
---
<header class="hero hero--simple">
  <div class="wrap hero-inner">
    <div>
      <p class="eyebrow">{{ metadata.title }}</p>
      <h1>{{ metadata.description }}</h1>
    </div>
  </div>
</header>
<main>
  <div class="wrap">
    <div class="postlist">
      {% for post in collections.posts %}
      <article class="postlist-item">
        <a href="{{ post.url }}">
          <time class="postmeta" datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
          <h2>{{ post.data.title }}</h2>
          {% if post.data.description %}<p>{{ post.data.description }}</p>{% endif %}
        </a>
      </article>
      {% endfor %}
    </div>
  </div>
</main>
